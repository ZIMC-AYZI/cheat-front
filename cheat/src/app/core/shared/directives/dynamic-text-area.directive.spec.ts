import { DynamicTextAreaDirective } from './dynamic-text-area.directive';

describe('DynamicTextAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTextAreaDirective();
    expect(directive).toBeTruthy();
  });
});
